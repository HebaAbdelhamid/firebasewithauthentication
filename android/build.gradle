
buildscript {
    ext.kotlin_version = '1.6.10'
    repositories {
        google()
        mavenCentral()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.0'  // Correct Android Gradle Plugin version
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.10"
    }
}
apply plugin: 'com.android.application'  // Use apply for Android plugin
apply plugin: 'kotlin-android'  // You can still apply Kotlin plugin in the old way

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = '../build'
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(':app')
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
